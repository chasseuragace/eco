name: shared_ecosystem
description: Shared utilities and frameworks for the development ecosystem
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0

dev_dependencies:
  test: ^1.21.0
